services:
    tesseract-admin-ui:
        container_name: tesseract-admin-ui
        build:
            context: .
            dockerfile: ./Dockerfile
            args:
                - BASE_URL=${NEXT_PUBLIC_API_URL}
        image: tesseract-admin-ui
        env_file: .env
        network_mode: host
        restart: always
